km = float(input("digite a distancia em km "))
velocidade = int(input("digite a velocidade em km/h"))

def percurso(km, velocidade):
    divisao_percurso = km / velocidade
    print("para fazer o percurso é necessário", divisao_percurso, "horas ")

percurso(km, velocidade)
    
    
    